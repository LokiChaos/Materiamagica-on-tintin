#nop reagents/get.tt

#gag {^You open};
#gag {^You close};
#gag {^You see nothing like that in};
#gag {^$};

/* For each bag that we can't empty incriment the counter by one */
_event_register {item_inv_noroom} {reagGet} {_reagNoRoom};

#alias {_reagNoRoom} {
	#nop %1;
	#math stateChar[have_reagents] {$stateChar[have_reagents] + 1};
}

#alias {_cleanReagGet} {
	#nop %1;
	_event_remove_specific {item_inv_noroom} {reagGet};
	#class tmp_reagent_get kill; 
}
